# Configuration Discord Bot
DISCORD_BOT_TOKEN=ton_token_bot_discord_ici
DISCORD_ANALYTICS_SECRET=ton_secret_pour_l_api

# Autres variables existantes
NEXT_PUBLIC_SUPABASE_URL=ton_url_supabase
SUPABASE_SERVICE_ROLE_KEY=ton_service_role_key
ADMIN_PASSWORD=ton_mot_de_passe_admin
DATAFAST_API_KEY=ton_api_key_datafast

# FirstPromoter (Affiliate)
# API key + Account ID: FirstPromoter → Settings → Integrations → API (private key / account id for API v2).
FIRSTPROMOTER_API_KEY=ton_api_key_firstpromoter
FIRSTPROMOTER_ACCOUNT_ID=ton_account_id_firstpromoter
# Optional: positive integer campaign id only (wrong or placeholder values often cause FirstPromoter HTTP 400).
# If you set a default campaign in the FirstPromoter UI for new promoters, you can leave this unset.
# Where to find the id: Campaigns → open your campaign → check the URL (…/campaigns/12345) or campaign settings;
# or use browser DevTools → Network while loading the campaign page and look for "campaign_id" / id in JSON responses.
FIRSTPROMOTER_INITIAL_CAMPAIGN_ID=123
# Optional: FirstPromoter welcome/drip emails for new promoters (true/false)
FIRSTPROMOTER_DRIP_EMAILS=false
# Vercel: after adding or changing these variables, redeploy Production so API routes receive them.
# If Vercel shows "Needs Attention" on a secret, rotate/update the value in Vercel and redeploy. Prefer marking API keys as Sensitive.

# AdsPower — Authenticator (TOTP) only for "Get the code" on app.ecomefficiency.com (Starter/Pro).
# JSON: AdsPower login email (lowercase) → Base32 secret (same as Google Authenticator / AdsPower member).
# Add one entry per mailbox you support. No email/IMAP path anymore.
# ADSPOWER_TOTP_BY_EMAIL_JSON={"admin@ecomefficiency.com":"YOUR_BASE32_SECRET"}
#
# Katabump / imap_server.js (optional — other tools may still use IMAP env vars on the mail server):
# IMAP_ADSPOWER_ADMIN_HOSTS=...
