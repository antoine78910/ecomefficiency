/**
 * Single canonical origin for SEO: sitemap, robots.txt, metadataBase, canonicals.
 * Must match Vercel redirects (Vercel redirects to www). One origin = Google ne change plus l’URL d’origine.
 */
export const CANONICAL_ORIGIN = "https://www.ecomefficiency.com";
