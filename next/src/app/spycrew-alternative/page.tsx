import { redirect } from "next/navigation";

/** SEO alias: canonical comparison lives at /alternatives/spycrew */
export default function SpyCrewAlternativeRedirectPage() {
  redirect("/alternatives/spycrew");
}
